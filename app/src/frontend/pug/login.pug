extends plugin

append content
    form(method="POST" action="/api/login").flex-column.gap-10px
        input(type="text" name="username" placeholder="username")
        input(type="text" name="password" placeholder="password")
        input(type="submit" value="login")